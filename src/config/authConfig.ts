// Authentication configuration
export const authConfig = {
  // Google Auth Configuration
  google: {
    // Replace these with your actual client IDs from Google Cloud Console
    webClientId: "YOUR_WEB_CLIENT_ID.apps.googleusercontent.com", // Web client ID
    iosClientId: "YOUR_IOS_CLIENT_ID.apps.googleusercontent.com", // iOS client ID
    androidClientId: "YOUR_ANDROID_CLIENT_ID.apps.googleusercontent.com", // Android client ID
  },
  // Add other auth providers here in the future (Facebook, Apple, etc.)
};